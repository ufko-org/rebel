; Test for function "forargs"
; ------------------------------------------------------------
(load "testing.rbl")

; Iteration / Control
; ------------------------------------------------------------
(test "1: forargs iterates over all arguments"
  (local (res)
    (set res '())
    (func (foo)
      (forargs (i)
        (push i res -1)
      )
    )
    (foo 1 2 3 4)
    res
  )
  '(1 2 3 4)
)

(test "2: forargs stops at break expression"
  (local (res)
    (set res '())
    (func (foo)
      (forargs (i (> i 2))   ; break when i > 2 is true
        (push i res -1)
      )
    )
    (foo 1 2 3 4)
    res
  )
  '(1 2)
)
