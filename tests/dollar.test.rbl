; Test for function "$"
; ------------------------------------------------------------
(load "testing.rbl")
(context 'test)

; Variables
; ------------------------------------------------------------
(test "1: retrieve $0 value"
  (let ($0 123)
  (= ($ 0) 123))
  true
)
