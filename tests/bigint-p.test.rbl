; tests for bigint? function
; (test "test-number: test-descrition"
;   tested-expression
;   expected-value)

(load "testing.rbl")
(context 'test)
;
(test "1: bigint?"
  (bigint? 12345L)
  true)

(test "2: bigint?"
  (bigint? 1)
  nil)

(test "3: bigint?"
  (bigint? "hello")
  nil)

