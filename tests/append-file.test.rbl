; Test for function "append-file"
; ------------------------------------------------------------
(load "testing.rbl")
(context 'test)

; ------------------------------------------------------------
(test "1: append string to file"
  (fappend "junk" "ABC")
  3
)

(test "2: append string to file using file:// URL"
  (fappend "file://junk" "DEF")
  3
)

(test "3: read file and check content"
  (= (fread "junk") "ABCDEF")
  true
)

; Post cleanup
; ------------------------------------------------------------
(if (file? "junk") (delete-file "junk"))
