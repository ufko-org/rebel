; Tests for function "rotate"
; ---------------------------------------------------------------------
(load "testing.rbl")

(test "1: rotate int list"
  (let (lst '(1 2 3 4))
    (rotate (copy lst)))
  '(4 1 2 3))

(test "2: rotate string list"
  (let (lst '("a" "b" "c"))
    (rotate (copy lst)))
  '("c" "a" "b"))

(test "3: rotate symbol list"
  (let (lst '(x y z))
    (rotate (copy lst)))
  '(z x y))


