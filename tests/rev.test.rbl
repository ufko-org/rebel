; Tests for function "rev"
; ---------------------------------------------------------------------
(load "testing.rbl")

(test "1: reverse int list"
  (let (lst '(1 2 3 4))
    (rev (copy lst)))
  '(4 3 2 1))

(test "2: reverse string list"
  (let (lst '("a" "b" "c"))
    (rev (copy lst)))
  '("c" "b" "a"))

(test "3: reverse symbol list"
  (let (lst '(x y z))
    (rev (copy lst)))
  '(z y x))


