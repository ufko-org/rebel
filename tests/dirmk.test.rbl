; Test for function "dirmk"
; ------------------------------------------------------------
(load "testing.rbl")

; Directory operations
; ------------------------------------------------------------
(test "1: create, change, revert and remove directory"
  (do
    (dirmk "adir")
    (dircd "adir")
    (dircd "..")
    (dirrm "adir")
  )
  true
)
