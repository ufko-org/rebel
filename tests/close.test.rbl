; Test for function "close"
; ------------------------------------------------------------
(load "testing.rbl")
(context 'test)

; File close operations
; ------------------------------------------------------------
(test "1: open and close file"
  (let (fd (open "tests-fs/simple/a.txt" "read"))
  (close fd))
  true
)
